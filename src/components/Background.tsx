import React from "react";

const Background = () => {
  return (
    <div className="absolute inset-0 bg-gray-800 flex items-center justify-center">
      <h1 className="text-9xl text-zinc-200 text-center">Docs</h1>
    </div>
  );
};

export default Background;
